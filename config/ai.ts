// ============================================================================
//  KHAN ORDA — КОНФИГ ИИ-ПОМОЩНИКА (агент-консьерж)
//  Провайдер-независимо: сейчас Gemini, позже Claude (сменить provider + env).
// ============================================================================

export const AI = {
  provider: (process.env.AI_PROVIDER as "gemini" | "claude") ?? "gemini",

  // Рабочая бесплатная модель на текущем ключе (gemini-2.0-flash даёт квоту 0).
  model: process.env.AI_MODEL ?? "gemini-flash-lite-latest",

  // Сколько раз агент может вызвать инструменты за один ответ (защита от циклов).
  maxToolIterations: 5,
};
