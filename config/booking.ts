// ============================================================================
//  KHAN ORDA — КОНФИГ БРОНИРОВАНИЯ
//  Значения по умолчанию для локальной разработки. В проде переопределяются
//  переменными окружения (.env.local / Vercel).
// ============================================================================

export const BOOKING_CONFIG = {
  /** Сколько минут держим слот в статусе pending, ожидая оплату. */
  holdMinutes: 30,

  /**
   * Kaspi Pay: ссылка/QR для оплаты предоплаты (100%). Пока плейсхолдер —
   * подставьте реальный платёжный линк Kaspi Pay бизнеса «ХАН ОРДА II».
   * Путь B (полу-автомат): гость платит по этой ссылке, админ подтверждает.
   */
  kaspiPayUrl: process.env.NEXT_PUBLIC_KASPI_PAY_URL ?? "",

  /**
   * Простой пароль для входа в админку (dev-заглушка). В проде задайте
   * ADMIN_PASSWORD в переменных окружения. Позже заменим на нормальную авторизацию.
   */
  adminPassword: process.env.ADMIN_PASSWORD ?? "khanorda",
};

/**
 * Реквизиты юрлица (для страницы оплаты/чека). Kaspi Pay — основной канал,
 * счёт БЦК — для сверки/договора эквайринга.
 */
export const MERCHANT = {
  legalName: 'ТОО «ХАН ОРДА II»',
  bin: "241140026345",
  bank: "АО «Банк ЦентрКредит»",
  iban: "KZ618562203142230947",
  swift: "KCJBKZKX",
};
